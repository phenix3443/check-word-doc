## DocxLint vNext base config - 所有文档必须遵循的基线规则
## 通过 import 机制组合各类别的基线规则

import:
  - "base/structure.yaml"
  - "base/references.yaml"
  - "base/paragraphs.yaml"
  - "base/headings.yaml"

version: "vnext"

# 页面格式基线规则（待实现）
# rules:
#   - id: "PGN001"
#     enabled: false
#     params:
#       description: "文档必须包含页码（待实现）"
#       emit_issue: false